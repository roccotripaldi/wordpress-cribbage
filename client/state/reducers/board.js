const defaultState = {
    opponentPeg1Pos: 0,
    opponentPeg2Pos: 0,
    playerPeg1Pos: 0,
    playerPeg2Pos: 0
};

const board = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default board;