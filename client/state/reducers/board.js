export const defaultState = {
    opponent: [ -1, 0],
    player: [ -1, 0 ]
};

const board = (state = defaultState, action) => {
    switch (action.type) {
        default:
            return state
    }
};

export default board;