export const defaultState = {
    items: [],
};

const debugLog = ( state = defaultState, action ) => {
    switch ( action.type ) {
        default:
            return state
    }
};

export default debugLog;